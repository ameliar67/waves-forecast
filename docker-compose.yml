name: waves-forecast

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      IS_DEVELOPMENT: "false"
      SCHEDULE: "15 4 * * *"
    secrets:
      - source: backend_secrets_env
        target: /app/.env

secrets:
  backend_secrets_env:
    file: ./backend.env
